<Page
    x:Class="ComponentVault.WinUI.Views.MovementsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:design="using:ComponentVault.WinUI.Design"
    xmlns:models="using:ComponentVault.WinUI.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=design:DesignMainViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24">
            <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="出入库记录" />
            <TextBlock Margin="0,6,0,20" Style="{StaticResource PageSubtitleTextStyle}" Text="查看本机保存的入库、出库与库存调整记录，并追踪每次库存变化。" />

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="16">
                    <CommandBar Background="Transparent" DefaultLabelPosition="Right">
                        <AppBarButton Click="OnRecordMovementClicked" Icon="Add" IsEnabled="{Binding CanRecordMovement}" Label="记录变动" />
                        <AppBarButton Click="OnRefreshFeedClicked" Icon="Refresh" Label="刷新列表" />
                    </CommandBar>

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Padding="14"
                            Background="{StaticResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{StaticResource VaultBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="入库" />
                                <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding InboundMovementCount}" />
                            </StackPanel>
                        </Border>

                        <Border
                            Grid.Column="1"
                            Padding="14"
                            Background="{StaticResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{StaticResource VaultBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="出库" />
                                <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding OutboundMovementCount}" />
                            </StackPanel>
                        </Border>

                        <Border
                            Grid.Column="2"
                            Padding="14"
                            Background="{StaticResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{StaticResource VaultBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                            <StackPanel Spacing="4">
                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="调整" />
                                <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding AdjustmentMovementCount}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock
                        Foreground="{StaticResource TextFillColorSecondaryBrush}"
                        Text="{Binding MovementFeedSummary}"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Foreground="{StaticResource TextFillColorSecondaryBrush}"
                        Text="{Binding StatusMessage}"
                        TextWrapping="Wrap" />

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="360" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="12">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="最近记录" />
                            <TextBlock
                                Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                Text="这里展示设备本地保存的最近变动，方便核对数量变化与操作原因。"
                                TextWrapping="Wrap" />

                            <ListView
                                x:Name="MovementsListView"
                                ItemsSource="{Binding Movements}"
                                SelectedItem="{Binding SelectedMovement, Mode=TwoWay}"
                                SelectionChanged="OnMovementSelectionChanged">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:StockMovementRecord">
                                        <Grid Padding="0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock FontWeight="SemiBold" Text="{Binding ComponentName}" />
                                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="{Binding ComponentSku}" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding MovementTypeLabel}" />
                                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="{Binding Reason}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding HappenedAt}" />
                                            <Border
                                                Grid.Column="3"
                                                Padding="10,6"
                                                Background="{StaticResource VaultAccentSoftBrush}"
                                                CornerRadius="999">
                                                <TextBlock Foreground="{StaticResource VaultAccentBrush}" Text="{Binding QuantityLabel}" />
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <Border Grid.Column="1" Style="{StaticResource InsetPanelStyle}">
                            <StackPanel Spacing="14">
                                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="变动详情" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding SelectedMovementTitle}" />
                                <TextBlock
                                    Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                    Text="{Binding SelectedMovementSubtitle}"
                                    TextWrapping="Wrap" />

                                <InfoBar
                                    IsOpen="True"
                                    IsClosable="False"
                                    Severity="{Binding SelectedMovementStatusSeverity}"
                                    Title="{Binding SelectedMovementStatusTitle}"
                                    Message="{Binding SelectedMovementStatusMessage}" />

                                <Border
                                    Padding="14"
                                    Background="{StaticResource VaultSurfaceBrush}"
                                    BorderBrush="{StaticResource VaultBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="14">
                                    <StackPanel Spacing="4">
                                        <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="数量变化" />
                                        <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding SelectedMovementQuantityText}" />
                                    </StackPanel>
                                </Border>

                                <StackPanel Spacing="10">
                                    <TextBlock FontWeight="SemiBold" Text="审计字段" />
                                    <Grid ColumnSpacing="12" RowSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="84" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="类型" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedMovementTypeText}" TextWrapping="Wrap" />

                                        <TextBlock Grid.Row="1" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="原因" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedMovementReason}" TextWrapping="Wrap" />

                                        <TextBlock Grid.Row="2" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="备注" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedMovementNote}" TextWrapping="Wrap" />

                                        <TextBlock Grid.Row="3" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="更新时间" />
                                        <TextBlock Grid.Row="3" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedMovementUpdatedAt}" TextWrapping="Wrap" />
                                    </Grid>
                                </StackPanel>

                                <TextBlock
                                    Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                    Text="{Binding SelectedMovementActionHint}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
