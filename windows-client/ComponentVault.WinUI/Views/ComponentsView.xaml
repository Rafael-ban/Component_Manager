<Page
    x:Class="ComponentVault.WinUI.Views.ComponentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:models="using:ComponentVault.WinUI.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24">
            <TextBlock FontSize="30" FontWeight="SemiBold" Text="Components" />
            <TextBlock Margin="0,6,0,20" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Inventory browsing and desktop-friendly detail density." />

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="16">
                    <CommandBar Background="Transparent" DefaultLabelPosition="Right">
                        <AppBarButton Click="OnAddComponentClicked" Icon="Add" Label="Add component" />
                        <AppBarButton Click="OnEditComponentClicked" Icon="Edit" Label="Edit selected" />
                        <AppBarButton Click="OnDeleteComponentClicked" Icon="Delete" Label="Soft delete" />
                    </CommandBar>

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <AutoSuggestBox
                            x:Name="ComponentSearchBox"
                            PlaceholderText="Search SKU, name, category, or location"
                            QueryIcon="Find"
                            TextChanged="OnSearchTextChanged" />

                        <ToggleSwitch
                            x:Name="LowStockOnlyToggle"
                            Grid.Column="1"
                            Header="Low stock only"
                            Toggled="OnLowStockOnlyToggled" />
                    </Grid>

                    <TextBlock
                        Foreground="{StaticResource TextFillColorSecondaryBrush}"
                        Text="{Binding ComponentInventorySummary}"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Foreground="{StaticResource TextFillColorSecondaryBrush}"
                        Text="{Binding StatusMessage}"
                        TextWrapping="Wrap" />

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="360" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="12">
                            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Inventory list" />
                            <TextBlock
                                Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                Text="Select a component to inspect package, location, and reorder posture."
                                TextWrapping="Wrap" />

                            <ListView
                                x:Name="ComponentsListView"
                                ItemsSource="{Binding Components}"
                                SelectedItem="{Binding SelectedComponent, Mode=TwoWay}"
                                SelectionChanged="OnComponentSelectionChanged"
                                SelectionMode="Single">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:ComponentRecord">
                                        <Border
                                            Margin="0,0,0,10"
                                            Padding="16"
                                            Background="{StaticResource VaultSurfaceBrush}"
                                            BorderBrush="{StaticResource VaultBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="16">
                                            <StackPanel Spacing="10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" />
                                                        <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="{Binding Sku}" />
                                                    </StackPanel>
                                                    <Border
                                                        Grid.Column="1"
                                                        Padding="10,6"
                                                        Background="{StaticResource VaultAccentSoftBrush}"
                                                        CornerRadius="999">
                                                        <TextBlock Foreground="{StaticResource VaultAccentBrush}" Text="{Binding Quantity}" />
                                                    </Border>
                                                </Grid>
                                                <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="{Binding Description}" TextWrapping="Wrap" />
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <Border Padding="10,6" Background="{StaticResource ControlFillColorSecondaryBrush}" CornerRadius="999">
                                                        <TextBlock Text="{Binding Location}" />
                                                    </Border>
                                                    <Border Padding="10,6" Background="{StaticResource VaultWarningSoftBrush}" CornerRadius="999">
                                                        <TextBlock Foreground="{StaticResource VaultWarningBrush}" Text="{Binding Status}" />
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <Border
                            Grid.Column="1"
                            Padding="18"
                            Background="{StaticResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{StaticResource VaultBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="18">
                            <StackPanel Spacing="14">
                                <TextBlock FontSize="20" FontWeight="SemiBold" Text="Selected component" />
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding SelectedComponentName}" />
                                <TextBlock
                                    Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                    Text="{Binding SelectedComponentSubtitle}"
                                    TextWrapping="Wrap" />

                                <InfoBar
                                    IsOpen="True"
                                    IsClosable="False"
                                    Severity="{Binding SelectedComponentStatusSeverity}"
                                    Title="{Binding SelectedComponentStatusTitle}"
                                    Message="{Binding SelectedComponentStatusMessage}" />

                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border
                                        Padding="14"
                                        Background="{StaticResource VaultSurfaceBrush}"
                                        BorderBrush="{StaticResource VaultBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="14">
                                        <StackPanel Spacing="4">
                                            <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Quantity" />
                                            <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding SelectedComponentQuantityText}" />
                                        </StackPanel>
                                    </Border>

                                    <Border
                                        Grid.Column="1"
                                        Padding="14"
                                        Background="{StaticResource VaultSurfaceBrush}"
                                        BorderBrush="{StaticResource VaultBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="14">
                                        <StackPanel Spacing="4">
                                            <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Minimum stock" />
                                            <TextBlock FontSize="24" FontWeight="Bold" Text="{Binding SelectedComponentMinStockText}" />
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <StackPanel Spacing="10">
                                    <TextBlock FontWeight="SemiBold" Text="Storage and sync detail" />
                                    <Grid ColumnSpacing="12" RowSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Location" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedComponentLocation}" TextWrapping="Wrap" />

                                        <TextBlock Grid.Row="1" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Updated" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedComponentUpdatedAt}" TextWrapping="Wrap" />

                                        <TextBlock Grid.Row="2" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Description" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" FontWeight="Medium" Text="{Binding SelectedComponentDescription}" TextWrapping="Wrap" />
                                    </Grid>
                                </StackPanel>

                                <TextBlock
                                    Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                    Text="{Binding SelectedComponentActionHint}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
